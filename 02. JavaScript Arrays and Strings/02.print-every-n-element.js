function action(array, n) {
    let returnArray = [];
    for (let i = 0; i < array.length; i += n) {
        returnArray.push(array[i]);
        array.filter
    }
    return returnArray;
}

action(['1','2','3','4','5'],6); // 1